# sources

What atlas is built from. Star counts snapshotted 2026-09-03.

## Top 10 (what was taken)

| source | url | stars | taken |
|---|---|---|---|
| obra/superpowers | https://github.com/obra/superpowers | 281k | brainstorming (trimmed to `scope`), writing-plans → `build` (plan), systematic-debugging → `build` (debug), verification-before-completion → `build` (prove), requesting/receiving-code-review → `review`, finishing-a-development-branch → `ship` |
| affaan-m/everything-claude-code | https://github.com/affaan-m/everything-claude-code | 247k | block-no-verify hook, KISS/DRY/YAGNI rules distilled into `core`; rejected: 80% coverage mandate, planning-doc quintuplet, 100+ agents |
| mattpocock/skills | https://github.com/mattpocock/skills | 2026-09 | `grilling`: a design tree worked in rounds, whole frontier asked at once, every question carrying a recommendation, facts found by the agent → `atlasme`, and `scope` when the loop meets an undecided design. `triage`: verify the claim before planning around it, check whether it is already implemented or already rejected, and say what is missing as answerable questions → `scope` (intake). `to-tickets`: a decomposition published as tickets carrying their blocking edges → `build` (plan, --tickets). `ship` (handoff): compact a session for the next agent, point at artifacts instead of restating them → `ship` (handoff). `retro`: improve the environment (navigation, automated checks, review rules, steering weight, tool economy, information access) → `ship` (environment). `tdd`: red before green, the failing run as the last chance to catch a wrong test → one clause in `build`. Second pass: `wayfinder`, `research`, `questionnaire`, `prototype` adopted as `howto`, `intel`, `question`, `prototype`, and `improve-codebase-architecture`'s deepening survey (deletion test, git-churn bias, report cards, CONTEXT.md nouns) → `improve` (deepen). Rejected: `to-spec` (a spec published to a tracker duplicates the goal card and the plan), `teach`, `wait-what`, `writing-*` (not development stages) |
| garrytan/gstack | https://github.com/garrytan/gstack | 131k | completion status protocol → `build` (prove), careful guardrails → `guard.sh`, ship checklist (trimmed from 76 KB to one page); rejected: skill size, telemetry, 23 personas |
| DietrichGebert/ponytail | https://github.com/DietrichGebert/ponytail | 2026-09 (second pass) | the ladder and the review format (`delete:` `stdlib:` `native:` `yagni:` `shrink:` plus atlas's `reuse:`, `net: -N lines`) → `core`, `review` (shrink step), `review` slop lens; SessionStart/SubagentStart injection pattern. Second pass, each backed by ponytail's own benchmark results: the operational root-cause directive (grep every caller, fix the shared function once; 0/6 → 6/6 on the trace-transfer probe) → `core` Bugs; the robust-variant rule (same-size stdlib options, take the edge-case-correct one) → `core` ladder; one runnable check for non-trivial logic where no test exists, never flagged as bloat (v4 hardening: no bloat creep) → `core` Tests, reviewer; ceiling comments with an upgrade trigger and the debt ledger → `ceiling:` marker, `scripts/debt.sh`, `review --debt`, `setup` (map); the platform-native lookup → `review/references/platform-native.md`; the opt-in subagent matcher → `ATLAS_SUBAGENT_MATCHER` in `core-context.sh`; the agentic benchmark method (real headless sessions, seeded files, deterministic scorers with good/bad references proven by `--selftest` before any spend, tests counted apart from bloat) → `evals/`. Rejected: intensity levels and mode tracking (atlas has one doctrine, not modes), the hardware-calibration rule (domain-specific), the gain scoreboard (benchmark medians are not a per-repo number, as ponytail itself says) |
| anthropics/claude-plugins-official | https://github.com/anthropics/claude-plugins-official | 36k | code-review parallel agents + confidence >= 80 filter → `review`, its `--comment` inline PR comments → `review --pr` (one thread per finding); code-simplifier → `simplify` and review's shrink step (shrink step); feature-dev explorer agents → `atlas-scout`; pr-review-toolkit silent-failure-hunter → `review` safety lens; plugin-dev structure |
| EveryInc/compound-engineering-plugin | https://github.com/EveryInc/compound-engineering-plugin | 25k | ce-plan output contract (direct / brief / artifact) → `build` (plan); ce-compound one-learning-per-run → `ship` (learn); lfg pipeline → the self-running loop; ce-simplify-code personas collapsed into a checklist |
| JuliusBrussee/caveman | https://github.com/JuliusBrussee/caveman | 72k | native-core reuse ladder → `core`; lean-build / surgical-patch / safe-refactor / migration → `build` modes; verify-and-stop → `build` (prove); cavecrew compressed subagent output → all three agents |
| taste (design taste skill) + frontend-design | local — Claude skills, no public repo | local | anti-slop for UI: concrete px/hex, no vague vibes → `references/ui-taste.md` |
| agent-workflow-orchestrator (own) | https://github.com/giulioleone097/agent-workflow-orchestrator | local | goal contract, owner contract + handoff, review-diff one-pass rule, elision rule, prove-work ladder, "## Code Review Rules" in AGENTS.md → `scope`, agents, `review`, `core`, `build` (prove), `ship` (learn) |

| karpathy/autoresearch | https://github.com/karpathy/autoresearch | 2026-09 | the keep/discard loop on one metric (one hypothesis, one experiment, one measurement), a fixed budget so runs compare, the agent confined to named files, a ledger of every attempt so nothing is retried → `optimize`. Rejected: the single-file rule as written (atlas takes `--paths`), the human-edited `program.md` (the skill is the program) |
| 666ghj/MiroFish | https://github.com/666ghj/MiroFish | 2026-09 | parallel simulated worlds from one seed, each run to its own outcome, then one agent reading the results back → `optimize` (one worktree and worker per hypothesis, the lead re-measures before a keep). Rejected: everything else, a social-simulation engine is not a development stage |
| anthropics/claude-code, ralph-wiggum plugin | https://github.com/anthropics/claude-code/tree/main/plugins/ralph-wiggum | 2026-09 | a hard iteration budget and an explicit completion condition on every loop → `optimize` (`--rounds`, target, two dry rounds). Rejected: the stop-hook re-prompt loop (atlas runs its rounds inside one skill, with a ledger the next session can read) |
| sweep of the same sources, 2026-09-09 | (see rows above) | 2026-09-09 | 17 mechanisms taken (worker `disallowedTools: Agent`, doctrine size cap in `check.sh`, hook dedupe up the tree on both events, lesson store read by `review`, history before cutting a guard, lint config as protected as tests, worker re-dispatch rule, grill checks claims about the code, worker cwd rule, compaction line, PR template floor, closing keyword only on full acceptance, model output as trust boundary, wide catch as safety finding, no stray notes files). Deferred, judged useful but below the bar: The pull request opens against the branch the work forked from, confirmed with the user wh; Pre-work inventory; `--pr` reads the PR's existing review threads before posting so an unmet earlier request b; An eval suite is a proof category like e2e; Delegation completion contract; A dev server started to take the UI proof picture runs detached with its output in a log f; A repo-wide audit hunts the dependency manifest for packages the stdlib or platform alread; Codex reads at most 32 KiB of project instructions (project_doc_max_bytes) and drops the t; Codex trusts a hook by the hash of its definition; A plan file carries a per-task completion line written as each task lands, so a compacted ; A changed CI workflow, pipeline or config file is proven by parsing it locally with a pars; Content that intake reads (issues, PR comments, pasted reports, images, handoff and grill ; Severity proof gate for reviewers; A finished or blocked subagent can be resumed with its full history through the host's mes; A directive that silences a check (lint-disable, ts-ignore, noqa, type; Pre-approve the read-only git and forge commands a skill names through the `allowed-tools` |
| humanlayer/skills | https://github.com/humanlayer/skills | 2026-09 | show-me: explain a change by its shape (pseudocode, call tree, file tree, component tree, one Mermaid sequence, diff-shaped before/after) → `ship` (dossier) shapes (`skills/ship/references/shapes.md`) |
| coldtea/pr-lens | https://github.com/coldteadotai/pr-lens | 2026-09 | comprehension layer, not another review bot: lanes as the reader's mental model, one hero edge, unchanged neighbours as the blast radius, C4-inspired view choice, no findings in a picture → `ship` (dossier) map and shape rules |
| e2e-testing | local — ~/.claude/skills | 2026-09 | one scenario per critical flow (happy + error path), data-testid selectors, deterministic waits, isolated setup/teardown, flake means fix the signal, recurring breakage becomes mandatory regression → `build` (e2e) and the doctrine's user-facing e2e proof gate |
| systematic-debugging | local — ~/.claude/skills | 2026-09 | hypothesis-tagged instrumentation `[DBG:H1:*]`, the browser logger-server for unreadable consoles, prefer live tools (devtools MCP, REPL) over added instrumentation, two-cycle escalation → `build` (debug) |

## Also read / rejected

- hamelsmu/claude-review-loop (https://github.com/hamelsmu/claude-review-loop) — stop-hook review loop; rejected as too intrusive.
- OneRedOak/claude-code-workflows (https://github.com/OneRedOak/claude-code-workflows) — design/security review lenses.
- github/spec-kit (https://github.com/github/spec-kit) and BMAD — rejected: ceremony.
| Anthropic, Claude Fable 5.1 guidance | claude-api skill: shared/model-migration.md (Fable 5.1 behavioral shifts, long-running agent recommendations), shared/prompt-audit.md | 2026-09 | de-prescription, grounded progress, assessment-not-fix, evidence before state changes, async delegation, memory surface, readable final report, the dated-pattern audit tables → `core/ATLAS.md`, the self-running loop, `ship` (dossier) |
| OpenAI, GPT-6 Astra and Codex | https://developers.openai.com/api/docs/guides/latest-model, https://developers.openai.com/codex/guides/agents-md, https://developers.openai.com/codex/skills, https://developers.openai.com/cookbook/examples/gpt-5/codex_prompting_guide | 2026-09 | user-over-skill precedence with the skill named when it blocks, bias to action, plan closure, test calibration, explicit delegation, description front-loading against Codex's 45-character shortening → `core/ATLAS.md`, every skill description, `scripts/check.sh` rule |
| pvncher, "Rethinking skills and prompts for GPT-6 Astra" | https://x.com/pvncher/status/2095991462416490862 | 2026-09 | skill descriptions as short as clarity allows (Codex shortens them when many are installed); the root SKILL.md as a router with progressive disclosure into references; no elaborate itineraries; AGENTS.md must not force reading docs or a repo map before every edit, pointers stay contextual; no test-nagging; explicit permission for safe local workflows; define completion up front → every description under 40 words, `ship/references/posting.md`, `review/references/audit.md`, the map rule reworded, the safe-checks line in `setup` |

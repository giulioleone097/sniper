# sources

What sniper is built from. Star counts snapshotted 2026-09-03.

## Top 10 (what was taken)

| source | url | stars | taken |
|---|---|---|---|
| obra/superpowers | https://github.com/obra/superpowers | 281k | brainstorming (trimmed to `scope`), writing-plans → `plan`, systematic-debugging → `debug`, verification-before-completion → `prove`, requesting/receiving-code-review → `review`, finishing-a-development-branch → `ship` |
| affaan-m/everything-claude-code | https://github.com/affaan-m/everything-claude-code | 247k | block-no-verify hook, KISS/DRY/YAGNI rules distilled into `core`; rejected: 80% coverage mandate, planning-doc quintuplet, 100+ agents |
| mattpocock/skills | https://github.com/mattpocock/skills | 2026-09 | `grilling`: a design tree worked in rounds, whole frontier asked at once, every question carrying a recommendation, facts found by the agent → `grill`. `triage`: verify the claim before planning around it, check whether it is already implemented or already rejected, and say what is missing as answerable questions → `intake`. `to-tickets`: a decomposition published as tickets carrying their blocking edges → `plan --tickets`. `handoff`: compact a session for the next agent, point at artifacts instead of restating them → `handoff`. `retro`: improve the environment (navigation, automated checks, review rules, steering weight, tool economy, information access) → `learn/references/environment.md`. `tdd`: red before green, the failing run as the last chance to catch a wrong test → one clause in `build`. Rejected: `to-spec` (a spec published to a tracker duplicates the goal card and the plan), `wayfinder` (multi-session mapping is `plan` plus `handoff`), `research` (a scout plus the host's fetch tools), `prototype` (throwaway spikes are product design, outside this cycle), `teach`, `wait-what`, `writing-*` (not development stages) |
| garrytan/gstack | https://github.com/garrytan/gstack | 131k | completion status protocol → `prove`, careful guardrails → `guard.sh`, ship checklist (trimmed from 76 KB to one page); rejected: skill size, telemetry, 23 personas |
| DietrichGebert/ponytail | https://github.com/DietrichGebert/ponytail | 2026-09 (second pass) | the ladder and the review format (`delete:` `stdlib:` `native:` `yagni:` `shrink:` plus sniper's `reuse:`, `net: -N lines`) → `core`, `simplify`, `review` slop lens; SessionStart/SubagentStart injection pattern. Second pass, each backed by ponytail's own benchmark results: the operational root-cause directive (grep every caller, fix the shared function once; 0/6 → 6/6 on the trace-transfer probe) → `core` Bugs; the robust-variant rule (same-size stdlib options, take the edge-case-correct one) → `core` ladder; one runnable check for non-trivial logic where no test exists, never flagged as bloat (v4 hardening: no bloat creep) → `core` Tests, reviewer; ceiling comments with an upgrade trigger and the debt ledger → `ceiling:` marker, `scripts/debt.sh`, `simplify --debt`, `map`; the platform-native lookup → `simplify/references/platform-native.md`; the opt-in subagent matcher → `SNIPER_SUBAGENT_MATCHER` in `core-context.sh`; the agentic benchmark method (real headless sessions, seeded files, deterministic scorers with good/bad references proven by `--selftest` before any spend, tests counted apart from bloat) → `evals/`. Rejected: intensity levels and mode tracking (sniper has one doctrine, not modes), the hardware-calibration rule (domain-specific), the gain scoreboard (benchmark medians are not a per-repo number, as ponytail itself says) |
| anthropics/claude-plugins-official | https://github.com/anthropics/claude-plugins-official | 36k | code-review parallel agents + confidence >= 80 filter → `review`; code-simplifier → `simplify`; feature-dev explorer agents → `sniper-scout`; pr-review-toolkit silent-failure-hunter → `review` safety lens; plugin-dev structure |
| EveryInc/compound-engineering-plugin | https://github.com/EveryInc/compound-engineering-plugin | 25k | ce-plan output contract (direct / brief / artifact) → `plan`; ce-compound one-learning-per-run → `learn`; lfg pipeline → `flow`; ce-simplify-code personas collapsed into a checklist |
| JuliusBrussee/caveman | https://github.com/JuliusBrussee/caveman | 72k | native-core reuse ladder → `core`; lean-build / surgical-patch / safe-refactor / migration → `build` modes; verify-and-stop → `prove`; cavecrew compressed subagent output → all three agents |
| taste (design taste skill) + frontend-design | local — Claude skills, no public repo | local | anti-slop for UI: concrete px/hex, no vague vibes → `references/ui-taste.md` |
| agent-workflow-orchestrator (own) | https://github.com/giulioleone097/agent-workflow-orchestrator | local | goal contract, owner contract + handoff, review-diff one-pass rule, elision rule, prove-work ladder, "## Code Review Rules" in AGENTS.md → `scope`, agents, `review`, `core`, `prove`, `learn` |

| humanlayer/skills | https://github.com/humanlayer/skills | 2026-09 | show-me: explain a change by its shape (pseudocode, call tree, file tree, component tree, one Mermaid sequence, diff-shaped before/after) → `narrate` shapes (`skills/narrate/references/shapes.md`) |
| coldtea/pr-lens | https://github.com/coldteadotai/pr-lens | 2026-09 | comprehension layer, not another review bot: lanes as the reader's mental model, one hero edge, unchanged neighbours as the blast radius, C4-inspired view choice, no findings in a picture → `narrate` map and shape rules |

## Also read / rejected

- hamelsmu/claude-review-loop (https://github.com/hamelsmu/claude-review-loop) — stop-hook review loop; rejected as too intrusive.
- OneRedOak/claude-code-workflows (https://github.com/OneRedOak/claude-code-workflows) — design/security review lenses.
- github/spec-kit (https://github.com/github/spec-kit) and BMAD — rejected: ceremony.
| Anthropic, Claude Fable 5.1 guidance | claude-api skill: shared/model-migration.md (Fable 5.1 behavioral shifts, long-running agent recommendations), shared/prompt-audit.md | 2026-09 | de-prescription, grounded progress, assessment-not-fix, evidence before state changes, async delegation, memory surface, readable final report, the dated-pattern audit tables → `core/SNIPER.md`, `flow`, `narrate` |
| OpenAI, GPT-6 Astra and Codex | https://developers.openai.com/api/docs/guides/latest-model, https://developers.openai.com/codex/guides/agents-md, https://developers.openai.com/codex/skills, https://developers.openai.com/cookbook/examples/gpt-5/codex_prompting_guide | 2026-09 | user-over-skill precedence with the skill named when it blocks, bias to action, plan closure, test calibration, explicit delegation, description front-loading against Codex's 45-character shortening → `core/SNIPER.md`, every skill description, `scripts/check.sh` rule |

interface:
  display_name: "Scope"
  short_description: "Turn any incoming work into a verified goal card, then build it"
  default_prompt: "Use $scope on this: read and verify the source if it is an issue or PR, settle open design branches with me, lock the goal card, then continue into build."
  brand_color: "#1A1A1A"
policy:
  allow_implicit_invocation: true

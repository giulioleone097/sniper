interface:
  display_name: "Build"
  short_description: "Implement the goal card at agreed seams, prove it, hand the diff to review"
  default_prompt: "Use $build on this card: plan if complex, debug if the cause is unknown, implement surgically, prove the acceptance check, then continue into review."
  brand_color: "#1A1A1A"
policy:
  allow_implicit_invocation: true

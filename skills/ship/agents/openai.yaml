interface:
  display_name: "Ship"
  short_description: "Commit proven work, write the dossier, keep the lesson; push or PR only if asked"
  default_prompt: "Use $ship: commit the proven changes as atomic commits; add --push or --pr only when I asked; --dossier for the approval dossier alone."
  brand_color: "#1A1A1A"
policy:
  allow_implicit_invocation: true
